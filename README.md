# App de tarefas feito com nextJs ( visual basiado no google tarefas )

## Requisitos
- Node

## Como rodar a aplicação
`ps: para mobile`
- execute o comando `npm i`
- execute o comando `npm run dev`
